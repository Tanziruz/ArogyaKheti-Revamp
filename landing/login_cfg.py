# from geopy.geocoders import Photon

# geolocator = Photon(user_agent="arogya_kheti_app")

def GetAddressDetails(pincode):
    # location = geolocator.geocode(str(pincode))
    return ("West Bengal", "India")

def GetCoordinates(pincode):
    # location = geolocator.geocode(str(pincode))
    return (22.5726, 88.3639)
